PORT=3000
UPLOAD_DIR=./uploads
# For local Docker setup, this URL works out-of-the-box. If using Neon, replace it with your cloud URL.
DATABASE_URL="postgresql://snapcheck:password@localhost:5432/snapcheck_db?schema=public"
# Optional: PlateRecognizer API token for better OCR fallback
PLATE_RECOGNIZER_TOKEN=""
